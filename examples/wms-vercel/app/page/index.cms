// Root URL `/` lands on the WMS dashboard.
// (The framework's auth bypass in runtime/script/web/router/page.cms passes
// `index` through unauthenticated so this redirect is reachable.)

inherits 'base'

function main()
    response.redirect( 'wms:main' )
end-function
